# This is the whole deployment. Push this file plus README.md to your
# ModelScope Studio repository, set the SDK to Docker, and press Launch.
#
# Everything - SillyTavern, the Cloudflare tunnel, backups, the control panel -
# lives inside the image below.
#
# Replace OWNER with the GitHub account that publishes the image, and pin a
# real version tag instead of :latest once you are past testing.

FROM ghcr.io/OWNER/st-modelscope:latest

# Optional: uncomment to pin behaviour without touching the Studio env-var UI.
# ENV TUNNEL_ENABLED=true
# ENV R2_ENABLED=false
